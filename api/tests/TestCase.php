<?php

namespace Tests;

use Illuminate\Foundation\Testing\TestCase as BaseTestCase;

/**
 * Base test case. Boots the Laravel application from bootstrap/app.php
 * with the phpunit.xml test env overrides applied. New tests should
 * extend this class — adding more here is fine but keep it generic.
 */
abstract class TestCase extends BaseTestCase
{
    //
}
