/**
 * Barrel export for the BookReady template platform module.
 *
 * Templates and the editor should import from this entry point, never
 * from the individual files, so a future refactor (e.g. splitting tokens
 * into spacing.ts / typography.ts) doesn't ripple through every consumer.
 */

export * from './tokens'
export * from './manifest'
